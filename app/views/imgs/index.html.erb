<h1>Listing imgs</h1>

<table>
  <tr>
    <th>Titulo</th>
    <th>Extension</th>
    <th>Proyecto</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @imgs.each do |img| %>
  <tr>
    <td><%= img.titulo %></td>
    <td><%= img.extension %></td>
    <td><%= img.proyecto_id %></td>
    <td><%= link_to 'Show', img %></td>
    <td><%= link_to 'Edit', edit_img_path(img) %></td>
    <td><%= link_to 'Destroy', img, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Img', new_img_path %>
