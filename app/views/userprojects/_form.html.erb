<%= form_for(@userproject) do |f| %>
  <% if @userproject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@userproject.errors.count, "error") %> prohibited this userproject from being saved:</h2>

      <ul>
      <% @userproject.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%=  f.hidden_field :usuario_id, :value => current_user.id %>
  </div>
  <div class="field">
    <% proy_id = params[:proyecto_id] %>
    <%=  f.hidden_field :proyecto_id, :value => proy_id %>
  </div>
  <div class="field">
    <%= f.label :monto %><br />
    <%= f.text_field :monto %>
  </div>
 
  <div class="field">
    <% fc = DateTime.now %>
    <%=  f.hidden_field :fecha_monto, :value =>fc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
